# 科研工作区

这是一个轻量的科研协作目录。目标是减少重复文书，把注意力放在三个问题上：研究问题是否真实、最近邻是否已经覆盖、下一项实验能否改变判断。

## 当前项目

- [20260902-CCFC选题](projects/20260902-CCFC选题/README.md)
- 当前状态：已冻结。GEAL 在预先固定的 LASO val discovery pair 上能够随 query 正确切换，CAGE/query-insensitive mechanism 主线停止。
- 保留内容：官方 baseline、LASO 数据审计、pair/query manifest、最小 switching 脚本和负结果。
- 当前下一步：确定新的可观察问题后再新建项目；不围绕 CAGE 的少量局部错误追加实验。

## 日常用法

进入一个项目时只读：

1. 根目录 [AGENTS.md](AGENTS.md)
2. 项目 `README.md`
3. 本次任务直接相关的论文、代码或实验文件

一次迭代默认只回答：

1. 新发现了什么？
2. 它改变了什么判断？
3. 下一步最小动作是什么？

不再维护阶段机、Gate 编号、证据数量、固定角色回传或每轮 artifact 清单。

## 研究循环

1. **定问题**：在项目 `README.md` 写清当前问题、为什么值得查、什么结果会让我们放弃。
2. **查最近邻**：先找最可能覆盖当前主张的工作；摘要用于筛选，方法和新颖性判断回到正文或官方代码。
3. **做最小判别**：优先做最便宜、最可能推翻假设的检查。单样本、10%/20% 子集和全量实验承担不同证据责任。
4. **再决定**：结果只导向继续、修改问题或停止。只有判断变化时才更新项目 `README.md` 或 `DECISIONS.md`。

文献影响核心主张时更新 `EVIDENCE.md`；实验真正开始后使用实验模板记录命令、配置、代码版本、数据划分和原始输出；准备写论文时再建立 claim-evidence 对照。

## 目录

- `projects/`：实际研究项目。每个项目的 `README.md` 是唯一状态入口。
- `docs/`：仍有参考价值的方法、课程和导师经验材料，按需读取。
- `templates/`：仅保留项目、证据、决策和实验四份轻量模板。
- `scripts/`：新建项目和轻量检查。
- `source_private/`：私密原始材料，不进入外部服务。

旧版本由 Git 历史保存，不在活动目录保留一套平行工作流。当前项目内部的 `archive/` 只保存仍有独特证据或讨论价值的研究材料，默认不读。

通用参考只保留三份，按需读取：

- [科研方法与轻量工作空间](docs/01_科研方法与轻量工作空间.md)
- [Agent 协作与研究审查](docs/02_Agent协作与研究审查.md)
- [科研决策、Gap 判断与论文实操](docs/03_科研决策、Gap判断与论文实操汇编.md)

## 新建项目

```powershell
PowerShell -ExecutionPolicy Bypass -File .\scripts\new_topic.ps1 `
  -Name "选题名称" -Venue "候选会议" -Deadline "YYYY-MM-DD"
```

脚本只生成 `README.md`、`EVIDENCE.md` 和 `DECISIONS.md`。实验和论文文件等真正需要时再创建。

## 保留的严谨性

精简的是行政流程，不是研究诚信。引用核验、公平 baseline、数据划分、实验可复现、负结果保留和投稿政策核对仍然必须执行，但只在相关任务发生时记录。
